// import { Message } from "ai";
// import { InputProps } from "~/remotion/video";
// import { getChapterScripts } from "./index";

// import { api } from "~/utils/api";

// export function createVideo(messagess: Message[]) {
//   const video: InputProps = {
//     chapters = [],
//   };

//   //Get the text outline for the video
//   const videoOutline = await getChapterScripts(messages);

// //   const audioMap = new Map<string, string>();
// //   Promise.all(
// //     videoOutline.chapters.map(async (chapter) => {
// //         chapter.slides.map(async (slide) => {

// //   for (let chapter of videoOutline.chapters) {
// //     video.chapters.push({
// //       slides: [
// //         ...chapter.slides.map((slide) => {
// //           return {
// //             durationInFrames: 0,
// //             audioUrl: "",
// //             faceVideoUrl: "",
// //             script: slide.script,
// //             slideType: slide.slideType,
// //           };
// //         }),
// //       ],
//     });
//   }
// }
